<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.LabelInfoMapper">

    <select id="selectListByApartmentId" resultType="com.atguigu.lease.model.entity.LabelInfo">
        select id,type,name
        from label_info
        where id in (
            select label_id
            from apartment_label
            where apartment_label.is_deleted =0 and apartment_id = #{id}
            ) and is_deleted = 0
    </select>
    <select id="selectListByRoomId" resultType="com.atguigu.lease.model.entity.LabelInfo">
        select li.id,
               li.type,
               li.name
        from label_info li
        where li.is_deleted = 0
          and id in (select label_id from room_label rl where rl.is_deleted = 0 and rl.room_id = #{id})
    </select>
</mapper>
