<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.LeaseAgreementMapper">

    <select id="selectListByRoomId" resultType="com.atguigu.lease.model.entity.LeaseTerm">
        select lt.id,
               lt.month_count,
               lt.unit
        from lease_term lt
        where lt.is_deleted = 0
        and lt.id in (select lease_term_id from room_lease_term rlt where rlt.is_deleted = 0
                                                        and rlt.room_id = #{id})
    </select>
</mapper>
